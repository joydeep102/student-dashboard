# Python
__pycache__/
*.py[cod]
venv/
.env

# Django
db.sqlite3
/media/
/staticfiles/
*.log

# Secrets (Google OAuth) — ignore real files, keep only templates/placeholders.
# Ignore the dir CONTENTS (not the dir) so the !exceptions below can re-include.
/secrets/*
!/secrets/.gitkeep
!/secrets/*.example.json

# OS / editor
.DS_Store
.idea/
.vscode/
