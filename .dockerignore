# Python / virtualenv
venv/
__pycache__/
*.py[cod]
*.egg-info/

# Local dev artifacts (don't bake these into the image)
db.sqlite3
media/
staticfiles/
*.log

# Secrets are mounted as a volume at runtime, never copied into the image
secrets/

# VCS / editor / OS
.git/
.gitignore
.idea/
.vscode/
.DS_Store

# Docker files themselves
Dockerfile
docker-compose.yml
.dockerignore
README.md
